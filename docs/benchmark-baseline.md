# 自動対戦ベースライン（実測）

最終更新: 2026-09-13

**注意:** 本結果は CPU 同士・同一時間条件での動作と傾向の確認である。  
対人勝率や「強さの確定評価」ではない。強化学習で強くなったことの証拠でもない。

機械可読の詳細: `docs/benchmark-output/benchmark-latest.json`  
自動生成の要約: `docs/benchmark-output/benchmark-latest.md`

---

## 実行条件

| 項目 | 値 |
|------|-----|
| 実行コマンド | `npm.cmd run sim:benchmark` |
| 組み合わせ | random vs random / max_flip vs random / random vs max_flip / max_flip vs max_flip |
| 各組み合わせ | 25 試合（合計 100） |
| 共通シード群 | 1000〜1024（組み合わせ間で同じシードを使用） |
| クールタイム | 双方 700 ms |
| 判断待ち | 双方 500 ms |
| 固定ステップ | 50 ms |
| 制限時間 | 180000 ms |
| 実行環境 | Windows、Node（開発マシン）、画面なし |
| 生成日時 (UTC) | 2026-09-13T10:01:38.585Z |

黒白の割り当てを入れ替えた対戦（max_flip vs random と random vs max_flip）を含む。

---

## 実測サマリ

| 組み合わせ | 黒勝 | 白勝 | 引分 | truncate | 平均石数(黒/白) | 平均ゲーム内時間 |
|------------|------|------|------|----------|-----------------|------------------|
| random vs random | 17 | 8 | 0 | 0 | 53.4 / 46.6 | 48420 ms |
| max_flip vs random | 16 | 7 | 2 | 0 | 52.4 / 44.8 | 46916 ms |
| random vs max_flip | 12 | 13 | 0 | 0 | 51.4 / 48.6 | 48422 ms |
| max_flip vs max_flip | 13 | 11 | 1 | 0 | 50.2 / 49.8 | 48388 ms |

### 全体処理

- 処理実時間（壁時計）: **8447.1 ms**
- 総ステップ数: **96173**
- 1秒あたりステップ数: **11385.3**
- 安全上限による中断: **0**（勝率集計から除外対象なし）

### 終了理由・不正

- ほとんどが `board_full`。`max_flip vs random` で `no_legal_moves` が 1 件。
- 同時着手競合による不成立: 組み合わせあたりおおよそ 24〜25 件（試合全体の合計）。
- 競合以外の不正要求: 各組み合わせ 0。

---

## 解釈（限定的）

- 同一アルゴリズム同士でも黒勝がやや多い場合があり、同時着手の優先や初期配置の非対称の影響が疑われる。確定結論にはしない。
- max_flip を白に回したときの白勝（13）は、黒ランダム相手では互角寄りの傾向に見えるが、25 試合のみのため確定評価ではない。

再実行:

```bash
npm.cmd run sim:benchmark
```
